#include "main.h"
/**
 * _abs - prints the abs value of an int
 * @n: the int in question
 * Return: the absolute value of an int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
